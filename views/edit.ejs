<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10  ">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
        </div>
       <h5 class="mb-5">Edit Your Post</h5>
       <form action="/update/<%= post._id %>" method="post">
        <textarea class="w-1/3 p-3 outline-none border-2 border-zinc-800  resize-none bg-transparent rounded-md block" name="content" placeholder="What`s on your mind......?"><%= post.content %></textarea>
        <input class="px-3 py-2 w-40 mt-2 text-black bg-yellow-500 block rounded-md" type="submit" value="Update post">
       </form>
    </div>
</body>
</html>